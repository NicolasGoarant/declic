<%# app/views/opportunities/new.html.erb %>

<div class="bg-gray-50 min-h-screen font-sans text-slate-800">
  <main class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10">

    <div class="mb-8 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
      <div>
        <h1 class="text-3xl font-extrabold tracking-tight text-slate-900 font-epilogue">
          Proposer une opportunité
        </h1>
        <p class="mt-2 text-slate-600">
          Partagez une initiative locale. Elle sera relue par l'équipe avant publication.
        </p>
      </div>
      <%= link_to "← Annuler", opportunities_path, class: "text-sm font-semibold text-slate-500 hover:text-slate-800 transition-colors" %>
    </div>

    <% if @opportunity.errors.any? %>
      <div class="rounded-xl border border-red-200 bg-red-50 p-4 mb-8">
        <div class="flex">
          <div class="shrink-0 text-red-400">⚠️</div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-red-800">
              Merci de corriger les <%= pluralize(@opportunity.errors.count, "erreur") %> :
            </h3>
            <ul class="mt-2 list-disc list-inside text-sm text-red-700">
              <% @opportunity.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    <% end %>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-10">
      <div class="lg:col-span-2">
        <%= form_with model: @opportunity, local: true, class: "space-y-8 bg-white p-8 rounded-3xl shadow-sm border border-slate-200" do |f| %>

          <div class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <%= f.label :title, "Titre de l'opportunité", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.text_field :title, placeholder: "Ex: Atelier compostage urbain", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
              <div>
                <%= f.label :category, "Catégorie", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.select :category,
                    [["🤝 Bénévolat", "benevolat"], ["🎓 Formation", "formation"], ["👋 Rencontres", "rencontres"], ["🚀 Entreprendre", "entreprendre"], ["🌱 Écologiser", "ecologiser"]],
                    { include_blank: "Choisir..." },
                    class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
            </div>

            <div>
              <%= f.label :description, "Description", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.text_area :description, rows: 5, placeholder: "Décrivez en quelques lignes votre initiative...", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>

            <div>
              <%= f.label :organization, "Organisation", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.text_field :organization, placeholder: "Ex: Maison de la Nature", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>

            <%# NOUVEAUX CHAMPS ADRESSE SÉPARÉS %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <%= f.label :address, "Adresse (Obligatoire)", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.text_field :address, required: true, placeholder: "Ex: 3 rue de la République", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
              <div>
                <%= f.label :city, "Ville (Obligatoire)", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.text_field :city, required: true, placeholder: "Nancy", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <%= f.label :postal_code, "Code Postal (Obligatoire)", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.text_field :postal_code, required: true, placeholder: "54000", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
              <div>
                <%= f.label :website, "Site web (optionnel)", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.url_field :website, placeholder: "https://...", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
            </div>

            <%# SECTION PHOTOS AVEC COMPRESSION %>
            <div class="field">
              <%= f.label :photos, "Ajouter des images (optionnel)", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <p class="text-xs text-slate-500 mb-2">Vous pouvez sélectionner plusieurs photos en une ou plusieurs fois</p>
              <%= f.file_field :photos, multiple: true, id: "photo-upload-input", class: "block w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-violet-50 file:text-violet-700 hover:file:bg-violet-100" %>
              <div id="image-preview-zone" class="mt-4 flex flex-wrap gap-3"></div>
            </div>

            <div>
              <%= f.label :contact_email, "Votre email de contact", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.email_field :contact_email, placeholder: "Pour vous recontacter si besoin", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>
          </div>

          <div class="pt-6 border-t border-slate-100">
            <%= f.submit "Valider et envoyer à Déclic", class: "w-full flex justify-center py-4 px-6 border border-transparent rounded-full shadow-lg text-lg font-bold text-white bg-gradient-to-r from-violet-600 to-indigo-600 hover:from-violet-700 hover:to-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-violet-500 transition-all transform hover:-translate-y-1" %>
          </div>
        <% end %>
      </div>

      <aside class="space-y-6">
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-6 mb-6">
          <h2 class="font-bold text-slate-900 flex items-center gap-2">
            <span class="text-xl">💡</span> Conseils
          </h2>
          <ul class="mt-4 space-y-2 text-sm text-slate-600">
            <li class="flex items-start gap-2">
              <span>✓</span>
              <span>Titre court et accrocheur</span>
            </li>
            <li class="flex items-start gap-2">
              <span>✓</span>
              <span>Description claire : qui, quoi, où, quand</span>
            </li>
            <li class="flex items-start gap-2">
              <span>✓</span>
              <span>Choisissez de belles images compressées automatiquement</span>
            </li>
          </ul>
        </div>
      </aside>
    </div>
  </main>
</div>

<%# SCRIPT DE COMPRESSION ET CUMUL D'IMAGES %>
<script>
  let allFiles = [];

  function compressImage(file, maxWidth = 1920, quality = 0.85) {
    return new Promise((resolve) => {
      if (!file.type.startsWith('image/')) {
        resolve(file);
        return;
      }

      const reader = new FileReader();
      reader.onload = function(e) {
        const img = new Image();
        img.onload = function() {
          const canvas = document.createElement('canvas');
          let width = img.width;
          let height = img.height;

          if (width > maxWidth) {
            height = (height * maxWidth) / width;
            width = maxWidth;
          }

          canvas.width = width;
          canvas.height = height;
          const ctx = canvas.getContext('2d');
          ctx.drawImage(img, 0, 0, width, height);

          canvas.toBlob(
            (blob) => {
              const compressedFile = new File([blob], file.name, {
                type: 'image/jpeg',
                lastModified: Date.now()
              });
              console.log(`📸 ${file.name}: ${(file.size / 1024 / 1024).toFixed(2)}MB → ${(compressedFile.size / 1024 / 1024).toFixed(2)}MB`);
              resolve(compressedFile);
            },
            'image/jpeg',
            quality
          );
        };
        img.src = e.target.result;
      };
      reader.readAsDataURL(file);
    });
  }

  document.addEventListener('change', async function(e) {
    if (e.target && e.target.id === 'photo-upload-input') {
      const input = e.target;
      const zone = document.getElementById('image-preview-zone');

      const loader = document.createElement('div');
      loader.className = 'text-sm text-violet-600 font-semibold animate-pulse';
      loader.textContent = '🔄 Compression...';
      zone.appendChild(loader);

      const newFiles = Array.from(input.files);
      const compressedFiles = await Promise.all(
        newFiles.map(file => compressImage(file))
      );

      loader.remove();

      compressedFiles.forEach(file => {
        const isDuplicate = allFiles.some(f => f.name === file.name && f.size === file.size);
        if (!isDuplicate) {
          allFiles.push(file);
        }
      });

      const dataTransfer = new DataTransfer();
      allFiles.forEach(file => dataTransfer.items.add(file));
      input.files = dataTransfer.files;

      zone.innerHTML = '';
      allFiles.forEach((file, index) => {
        const reader = new FileReader();
        reader.onload = function(event) {
          const wrapper = document.createElement('div');
          wrapper.className = "relative group";

          const img = document.createElement('img');
          img.src = event.target.result;
          img.className = "h-24 w-24 object-cover rounded-xl border-2 border-slate-200 shadow-md";

          const sizeBadge = document.createElement('div');
          const sizeMB = (file.size / 1024 / 1024).toFixed(1);
          sizeBadge.className = "absolute -top-2 -left-2 bg-green-500 text-white text-xs px-2 py-0.5 rounded-full font-bold";
          sizeBadge.textContent = `${sizeMB}MB`;

          const deleteBtn = document.createElement('button');
          deleteBtn.type = 'button';
          deleteBtn.innerHTML = '×';
          deleteBtn.className = "absolute -top-2 -right-2 w-6 h-6 bg-red-500 text-white rounded-full font-bold opacity-0 group-hover:opacity-100 transition-opacity hover:bg-red-600";
          deleteBtn.onclick = function() {
            allFiles.splice(index, 1);
            const dt = new DataTransfer();
            allFiles.forEach(f => dt.items.add(f));
            input.files = dt.files;
            wrapper.remove();
            if (allFiles.length > 0) {
              zone.innerHTML = '';
              input.dispatchEvent(new Event('change'));
            }
          };

          wrapper.appendChild(sizeBadge);
          wrapper.appendChild(img);
          wrapper.appendChild(deleteBtn);
          zone.appendChild(wrapper);
        }
        reader.readAsDataURL(file);
      });
    }
  });
</script>
