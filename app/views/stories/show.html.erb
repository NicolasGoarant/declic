<%= render "shared/navbar" %>

<main class="max-w-6xl mx-auto px-6 py-8 pt-20 md:pt-24">
  <div class="mb-4">
    <%= link_to "← Retour aux belles histoires", stories_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>

  <div class="grid grid-cols-1 gap-7 lg:grid-cols-[minmax(0,2fr)_minmax(0,1fr)]">
    <section class="min-w-0">
      <% img = @story.image_url.present? ? asset_path(@story.image_url) : @story.image_url.presence || 'https://images.unsplash.com/photo-1500648767791-00dcc994a43e?q=80&w=1600&auto=format&fit=crop' %>
      <div class="rounded-2xl overflow-hidden shadow">
        <img src="<%= img %>" class="w-full h-64 sm:h-72 object-cover" alt="<%= @story.title %>">
      </div>

      <div class="mt-4">
        <span class="inline-block text-xs font-semibold px-2 py-1 rounded-full bg-violet-100 text-violet-700">Belle histoire</span>
        <h1 class="mt-2 text-3xl sm:text-4xl font-extrabold leading-tight"><%= @story.title %></h1>
      </div>

      <% if @story.chapo.present? %>
        <div class="mt-4 bg-white rounded-2xl border border-gray-100 shadow p-5">
          <p class="text-lg text-gray-800 font-medium"><%= @story.chapo %></p>
        </div>
      <% end %>

      <div class="mt-4 bg-white rounded-2xl border border-gray-100 shadow p-6">
        <% if @story.body.present? %>
          <!-- Texte long saisi dans la BDD (Markdown/retours chariot supportés) -->
          <div class="prose max-w-none">
            <%= simple_format(@story.body) %>
          </div>
        <% else %>
          <!-- Fallback : résumé étendu + petit extrait légal -->
          <div class="prose max-w-none">
            <p><%= @story.description %></p>
          </div>

          <% if @story.respond_to?(:quote) && @story.quote.present? %>
            <figure class="mt-6 border-l-4 border-violet-300 pl-4 italic text-gray-700">
              <p>“<%= @story.quote %>”</p>
            </figure>
          <% end %>


          <% if @story.source_url.present? %>
            <blockquote class="mt-4 border-l-4 border-violet-300 pl-4 text-gray-700 italic">
              <%= safe_excerpt(@story.description.presence || "", words: 25) %>…
            </blockquote>
          <% end %>
        <% end %>

        <% if @story.source_url.present? %>
          <div class="mt-6 flex items-center gap-3">
            <%= link_to (@story.source_name.presence || "Source / plus d’infos"),
                        @story.source_url,
                        class: "inline-flex items-center px-4 py-2 rounded-lg bg-indigo-600 text-white hover:bg-indigo-700",
                        target: "_blank", rel: "noopener" %>
          </div>
        <% end %>
      </div>
    </section>

    <aside class="min-w-0 lg:sticky lg:top-24">
      <div class="bg-white rounded-2xl border border-gray-100 shadow p-5">
        <h3 class="font-semibold mb-3">Infos pratiques</h3>
        <ul class="space-y-3 text-sm text-gray-700">
          <% if @story.location.present? %>
            <li class="flex items-start gap-2"><span>📍</span><span><%= @story.location %></span></li>
          <% end %>
          <% if @story.source_url.present? %>
            <li class="flex items-start gap-2"><span>🔗</span><%= link_to (@story.source_name.presence || "Site / article"), @story.source_url, class: "text-indigo-600 hover:underline", target: "_blank", rel: "noopener" %></li>
          <% end %>
        </ul>

        <% if @story.latitude && @story.longitude %>
          <div class="mt-5 rounded-lg overflow-hidden border">
            <div id="mini-map" class="h-48 w-full"></div>
          </div>
        <% end %>
      </div>
    </aside>
  </div>
</main>


<% if @story.latitude && @story.longitude %>
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script>
    document.addEventListener("turbo:load", initMap); document.addEventListener("DOMContentLoaded", initMap);
    function initMap(){
      var el = document.getElementById('mini-map'); if(!el || el.dataset.ready) return;
      var lat = <%= @story.latitude.to_f %>, lng = <%= @story.longitude.to_f %>;
      var map = L.map(el, {zoomControl:true, scrollWheelZoom:false}).setView([lat,lng], 13);
      L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {attribution:'© OpenStreetMap'}).addTo(map);
      L.marker([lat,lng], { icon: L.divIcon({className:'', html:`<div class="picon" style="--c:#a855f7"><span>📖</span></div>`, iconSize:[26,32], iconAnchor:[13,28]}) }).addTo(map);
      el.dataset.ready = "1";
    }
  </script>
  <style>
    .picon{ width:26px;height:26px;border-radius:9999px;border:2px solid #fff;display:grid;place-items:center;
      color:#fff;font-size:14px;box-shadow:0 1px 4px rgba(0,0,0,.25);position:relative;background:#a855f7; }
    .picon::after{ content:"";position:absolute;left:50%;bottom:-6px;transform:translateX(-50%);
      border:6px solid transparent;border-top-color:#a855f7; }
  </style>
<% end %>
