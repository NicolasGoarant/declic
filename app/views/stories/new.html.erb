<%# app/views/stories/new.html.erb %>

<div class="bg-gray-50 min-h-screen font-sans text-slate-800">
  <main class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10">

    <div class="mb-8 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
      <div>
        <h1 class="text-3xl font-extrabold tracking-tight text-slate-900 font-epilogue">
          Proposer un témoignage
        </h1>
        <p class="mt-2 text-slate-600">
          Partagez votre parcours de vie ou votre reconversion.
          Elle sera relue par l'équipe avant publication.
        </p>
      </div>
      <%= link_to "← Annuler", stories_path, class: "text-sm font-semibold text-slate-500 hover:text-slate-800 transition-colors" %>
    </div>

    <% if @story.errors.any? %>
      <div class="rounded-xl border border-red-200 bg-red-50 p-4 mb-8">
        <div class="flex">
          <div class="shrink-0 text-red-400">⚠️</div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-red-800">
              Merci de corriger les <%= pluralize(@story.errors.count, "erreur") %> :
            </h3>
            <ul class="mt-2 text-sm text-red-700 list-disc list-inside">
              <% @story.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    <% end %>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-10">
      <div class="lg:col-span-2">
        <%= form_with model: @story, local: true, class: "space-y-8 bg-white p-8 rounded-3xl shadow-sm border border-slate-200" do |f| %>

          <div class="space-y-6">
            <div>
              <%= f.label :title, "Le titre de votre histoire", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.text_field :title, placeholder: "Ex: De la finance à la permaculture...", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>

            <div>
              <%= f.label :chapo, "Résumé court (en quelques lignes)", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.text_area :chapo, rows: 3, placeholder: "Présentez brièvement le sujet...", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>

            <div>
              <%= f.label :body, "Votre récit", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.text_area :body, rows: 12, placeholder: "Racontez-nous tout...", class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <%= f.label :author_name, "Votre nom / Pseudo", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.text_field :author_name, class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
              <div>
                <%= f.label :city, "Ville / Région", class: "block text-sm font-bold text-slate-700 mb-1" %>
                <%= f.text_field :city, class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
              </div>
            </div>

            <div>
              <%= f.label :author_email, "Votre email (ne sera pas publié)", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.email_field :author_email, class: "w-full rounded-xl border-slate-200 focus:border-violet-500 focus:ring-violet-500 shadow-sm" %>
            </div>

            <div>
              <%= f.label :photos, "Ajouter des photos", class: "block text-sm font-bold text-slate-700 mb-1" %>
              <%= f.file_field :photos, multiple: true, class: "block w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-violet-50 file:text-violet-700 hover:file:bg-violet-100" %>
            </div>
          </div>

          <div class="pt-6 border-t border-slate-100">
            <%= f.submit "Valider et envoyer à Déclic", class: "w-full flex justify-center py-4 px-6 border border-transparent rounded-full shadow-lg text-lg font-bold text-white bg-gradient-to-r from-violet-600 to-indigo-600 hover:from-violet-700 hover:to-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-violet-500 transition-all transform hover:-translate-y-1" %>
          </div>
        <% end %>
      </div>

      <div class="space-y-6">
        <div class="bg-white p-6 rounded-3xl shadow-sm border border-slate-200">
          <h3 class="font-bold text-slate-900 mb-4">Conseils de rédaction</h3>
          <ul class="text-sm text-slate-600 space-y-3">
            <li class="flex gap-2">
              <span class="text-violet-500 font-bold">•</span>
              <span>Racontez la bascule : Qu'est-ce qui a été le vrai *déclic* ?</span>
            </li>
            <li class="flex gap-2">
              <span class="text-violet-500 font-bold">•</span>
              <span>Soyez honnête sur les difficultés rencontrées.</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </main>
</div>
